include "application.conf"

play.server.pidfile.path = "/dev/null"
play.http.secret.key = "L4PSb0cxSlvekLFmErLOofFzfMpcNqutjwwoCimb4NKDie+f7NzsbvV7LFe/hf4V"

# ── Docker networking overrides ──────────────────────────────────
# Hostnames use Docker service names instead of 127.0.0.1

akka.contrib.persistence.mongodb.mongo.mongouri = "mongodb://mongodb:27017/cqrs-demo-journal?maxPoolSize=20&w=majority"

jdbc-connection-settings.url = "jdbc:mysql://mysql:3306/cqrs_jdbc_demo?allowPublicKeyRetrieval=true&noAccessToProcedureBodies=true&createDatabaseIfNotExist=true&m=yes&characterEncoding=UTF-8&connectTimeout=300000&useSSL=false&serverTimezone=UTC&socketTimeout=30000&autoReconnect=true&maxReconnects=10&initialTimeout=10"

clustering {
  ip = "0.0.0.0"
  seed-ip = "app"
}

# Artery canonical hostname must be the reachable service name, not 0.0.0.0
akka.remote.artery.canonical.hostname = "app"

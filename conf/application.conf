# https://www.playframework.com/documentation/latest/Configuration
include "projection.conf"
include "persistence.conf"
include "serialization.conf"
include "cluster.conf"

akka {
  loglevel = DEBUG
}

# Port isolation: this app listens on 9806 (HTTP) / 25561 (artery) so it never
# collides with the other concurrently-running project (9000 / 2551 / 8558 band).
play.server.http.port = 9806
play.server.http.address = "0.0.0.0"

play.modules.enabled += "net.imadz.modules.BootstrapModule"
play.modules.enabled += "scalikejdbc.PlayModule"
play.modules.disabled += "play.api.db.DBModule"
play.filters.enabled=[controllers.filter.LoggingFilter]
play.server.websocket.periodic-keep-alive-max-idle = 10 seconds

play.temporaryFile {
  reaper {
    enabled = true
    initialDelay = "5 minutes"
    interval = "30 seconds"
    olderThan = "30 minutes"
  }
}


架构重构指南：从“影子状态”向“纯粹 CQRS 投影”演进
🎯 重构背景与目标 (Context & Objective)
当前 Fab Demo 的 WebSocket UI 状态更新机制存在架构缺陷：使用了双重链路（Dual Pipelines）。FabDemoPipeline 和 PipelineStages 在内存中维护了一个虚假的“影子状态（Shadow State）”并直接推送给前端，导致 UI 状态与底层 EventSourcing 的真实 Journal 存在竞态条件且事件数量不一致。

本次重构目标：

消除影子链路： 剥夺 Pipeline 过程管理器（Process Manager）直接组装并推送 AggregateStateUpdated 给 UI 的职责。

建立统一投影： 基于 CQRS 原则，通过唯一的 Tag ("fab-view") 拉取所有相关聚合（Lot, Saga 等）的真实事件，在 Read-Side 重建聚合视图，并由该单一真实数据源向 WebSocket 推送状态。

🛠️ 执行步骤 (Execution Steps)
Step 1: 为聚合端事件打上统一视图标签 (Write-Side Tagging)
任务描述： 拦截所有的持久化聚合事件，为其添加全局视图 Tag。

修改文件： 找到 LotAggregate 的行为定义文件（如 LotBehaviors.scala 或 LotAggregate.scala 中的 EventSourcedBehavior 构建处）。

代码变更： 在 EventSourcedBehavior 的末尾追加 .withTagger，给所有事件打上 "fab-view" 标签。

Scala
EventSourcedBehavior[LotCommand, LotEvent, LotState](
persistenceId = PersistenceId(LotEntityTypeKey.name, lotId),
emptyState = LotEntity.empty(lotId),
commandHandler = commandHandler(ctx),
eventHandler = eventHandler
).withTagger(_ => Set("fab-view", "Lot")) // 保留原有的 Tag 逻辑，增加 fab-view
Saga 适配： 同样地，找到 SagaTransactionCoordinator 的行为定义文件，为其追加相同的 .withTagger(_ => Set("fab-view", "Saga"))。

Step 2: 建立统一视图投影 (Read-Side Projection)
任务描述： 创建一个全新的 Projection，订阅 "fab-view" 标签，负责重建 UI 树状图状态。

创建新文件： app/net/imadz/fab/projection/FabDemoViewHandler.scala

职责： 继承 JdbcHandler[EventEnvelope[Any], ScalikeJdbcSession]。

逻辑： 维护一个在内存中的 Map[String, LotStateSnapshot]。当匹配到 LotEvent（如 WaferClassified, LotCreated）或 SagaProgressEvent 时，更新这个 Map，并调用构造函数传入的 publishToUI: FabSimulationEvent => Unit 方法，推送最新的 AggregateStateUpdated。

创建新文件： app/net/imadz/fab/projection/FabDemoViewProjection.scala

职责： 使用 EventSourcedProvider.eventsByTag[Any](system, MongoReadJournal.Identifier, "fab-view") 拉取事件。

逻辑： 组装 JdbcProjection.exactlyOnce 并挂载 FabDemoViewHandler。

Step 3: 实施“外科手术”——斩断影子链路 (Cut off Shadow Pipeline)
任务描述： 移除 Pipeline 中所有“越权”的 UI 状态计算与推送代码，使其退化为纯粹的 Command 调度器。

修改文件： app/net/imadz/fab/chain/PipelineStages.scala

删除方法： 完全删除 def buildAggregateState(...) 方法。

清理推送： 搜索该文件中所有的 ctx.publisher(buildAggregateState(...)) 或是组装 AggregateStateUpdated 然后 publish 的代码，全部删除。

保留指令： 务必保留所有向聚合发指令的代码（例如 ctx.lotRef ! RecordWaferClassified(...)）。

修改文件： app/net/imadz/fab/chain/FabDemoPipeline.scala

清理推送： 同样清理掉所有直接 publish 聚合状态和 Wafer 状态组合的逻辑。

Step 4: 重建 WebSocket 桥接路由 (Wiring)
任务描述： 将新创建的投影与现有的 WebSocket hub 连接起来。

修改文件： app/controllers/FabDemoController.scala (或其他启动 Fab Demo Projection 的引导类/Bootstrap)。

代码变更： 在系统初始化时（或者 Demo 开始时），启动 FabDemoViewProjection，并将 publishEvent（向 WebSocket 广播的方法）作为参数注入到 FabDemoViewProjection 中。

注意防抖（Debounce）： 如果事件流非常密集，可以在推向 WebSocket 前加一个简单的限流或防抖处理，防止前端渲染过载。

🧪 验收标准 (Acceptance Criteria)
请在执行完上述修改后，确保满足以下条件：

编译通过，无未引用的残余代码。

FabDemoPipeline 中不再有针对 AggregateStateUpdated 的主动构造和推送。

AggregateStateUpdated 事件完全由 FabDemoViewHandler 根据 EventEnvelope 被动触发。

前端 UI 依然能正常绘制多级批次（Source Lot, Rework Lot, Pilot Lot 等），且动画基于 CQRS 的真实延迟展现。